import { NavBar } from "@/components/Landing/NavBar";
import { BookSection } from "@/components/Study/BookSection";

const page = () => {
  return (
    <section>
      <NavBar />
      <BookSection />
    </section>
  );
};
export default page;
